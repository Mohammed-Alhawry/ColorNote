<UserControl x:Class="ColorNote.View.NotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ColorNote.View"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <UserControl.Resources>
        <DataTemplate x:Key="NoteDataTemplate">
            <DockPanel>
                <TextBlock Text="{Binding Title}" />
                <TextBlock Grid.Column="1"
                           Text="{Binding Date, Converter={StaticResource DateTimeToOnlyDateConv}}"
                           Margin="20 0 0 0" Foreground="Gray" />
            </DockPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListView Grid.ColumnSpan="2" ItemsSource="{Binding Notes}"
                  SelectedItem="{Binding SelectedNote}"
                  ItemTemplate="{StaticResource NoteDataTemplate}" />
        <Button Grid.Row="1" Command="{Binding AddNoteCommand}"
                CommandParameter="{Binding RelativeSource={
             RelativeSource AncestorType={x:Type Window}}}">Add note</Button>
        <Button Grid.Column="1" Grid.Row="1" Command="{Binding DeleteNoteCommand}">Delete note</Button>
    </Grid>
</UserControl>